spring:
  application:
    name: gamelibrary

  datasource:
    url: jdbc:postgresql://localhost:5431/gamelibrary
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true

  flyway:
    enable: true
    locations: classpath:db/migration